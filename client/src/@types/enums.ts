export enum LOCALES {
  EN = "en-us",
  ID = "id-id",
}

export enum OVERVIEW {
  OPEN = "1. open",
  HIGH = "2. high",
  LOW = "3. low",
  CLOSE = "4. close",
  VOLUME = "5. volume",
}

export enum LOGIN {
  EMAIL = "EMAIL",
  USERNAME = "USERNAME",
}
