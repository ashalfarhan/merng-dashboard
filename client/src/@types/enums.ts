export enum LOCALES {
  EN = "en-uk",
  ID = "id-id",
}

export enum SALES {
  AMOUNT = "amount",
  INCOME = "income",
}

export enum INVENTORY {
  AMOUNT = "amount",
  SPENDS = "spends",
}

export enum LOGIN {
  EMAIL = "EMAIL",
  USERNAME = "USERNAME",
}
